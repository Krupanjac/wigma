# ─── Wigma Backend Environment Variables ────────────────────────────────────
# Copy this file to .env and fill in your values.
#   cp .env.example .env
#
# These are consumed by:
#   1. docker-compose.yml  (passed to the C++ WS server container)
#   2. The C++ server reads them via std::getenv() in config.cpp
# ─────────────────────────────────────────────────────────────────────────────

# ── Supabase (required) ─────────────────────────────────────────────────────
# Project URL — same as the frontend uses
SUPABASE_URL=Your_supabase_url_here

# Service Role Key — found in Supabase Dashboard → Settings → API → service_role
# ⚠ This key bypasses RLS! Never expose it client-side.
SUPABASE_SERVICE_KEY=your_service_role_key_here

# JWT Secret — found in Supabase Dashboard → Settings → API → JWT Secret
# Used by the C++ server to verify auth tokens without calling Supabase.
JWT_SECRET=your_jwt_secret_here

# ── WebSocket Server ────────────────────────────────────────────────────────
WS_PORT=9001
MAX_ROOMS=1024
MAX_PEERS=64
SNAPSHOT_INTERVAL_MS=60000
